name: Spellcheck

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  spellcheck:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v5.0.1
        
      - name: Spell Check
        uses: igsekor/pyspelling-any@v0.02
        
          
          
          
